# Glean MCP — Admin Console → Platform → Glean MCP server
GLEAN_MCP_URL=https://rudyonboarding-be.glean.com/mcp/default

# Optional dev bypass — leave empty to use OAuth login instead
GLEAN_MCP_TOKEN=

# OAuth (Dynamic Client Registration + PKCE)
FRONTEND_URL=http://localhost:5174
OAUTH_REDIRECT_URI=http://localhost:8001/api/auth/callback
OAUTH_CLIENT_NAME=MCP Chatbot
SESSION_SECRET=change-me-to-a-random-string

MCP_TOOL=chat
HOST=0.0.0.0
PORT=8001
